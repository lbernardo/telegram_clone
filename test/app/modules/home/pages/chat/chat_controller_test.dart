import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:telegram/app/modules/home/pages/chat/chat_controller.dart';
import 'package:telegram/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // ChatController chat;
  //
  setUp(() {
    //     chat = HomeModule.to.get<ChatController>();
  });

  group('ChatController Test', () {
    //   test("First Test", () {
    //     expect(chat, isInstanceOf<ChatController>());
    //   });

    //   test("Set Value", () {
    //     expect(chat.value, equals(0));
    //     chat.increment();
    //     expect(chat.value, equals(1));
    //   });
  });
}
